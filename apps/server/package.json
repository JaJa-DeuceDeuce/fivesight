{
  "name": "@fivesight/server",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --compile --minify-whitespace --minify-syntax --target bun --outfile dist/server src/index.ts && rm -f .*.bun-build *.bun-build"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "@fivesight/typescript-config": "workspace:*",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
